# Ursprünglicher Projektstand

Die Dateien hier stammen aus deinem übergebenen Textexport. `90_uebungen/main_1.py` und die ursprüngliche README wurden inhaltlich erhalten. Der Textexport enthält keine leeren Ordner und keine ausgelassenen Dateitypen; weitere vorhandene Projektdateien lassen sich daraus nicht rekonstruieren.

Die alte Übung verwendet globale Daten, fragt beim Start nach einer Eingabe und bearbeitet feste Listenpositionen. Sie bleibt als Lernstand erhalten. Der überarbeitete Ablauf liegt unter [80_praxis/notizen](../80_praxis/notizen/README.md).
